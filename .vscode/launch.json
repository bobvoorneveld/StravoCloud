{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Run",
            "program": "${workspaceFolder:StravoCloud}/.build/debug/Run",
            "args": [],
            "cwd": "${workspaceFolder:StravoCloud}",
            "preLaunchTask": "swift: Build Debug Run"
        },
        {
            "type": "lldb",
            "request": "launch",
            "name": "Release Run",
            "program": "${workspaceFolder:StravoCloud}/.build/release/Run",
            "args": [],
            "cwd": "${workspaceFolder:StravoCloud}",
            "preLaunchTask": "swift: Build Release Run"
        }
    ]
}
